import java.lang.Math;

public class maths {
    public static void main(String[] args) {
        int n = 40;
        int s = 50;
        Math.max(n, s);
        Math.min(n, s);
        int rondomNumber = (int) (Math.random() * 100);
        System.out.println(rondomNumber);

    }
}